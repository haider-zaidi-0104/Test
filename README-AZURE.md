# Syed Ali Raza — Cool Cyan / Violet Portfolio

Azure Static Website ready. Upload `index.html`, `styles.css`, `script.js` and `assets/Resume.pdf` to the `$web` container.

Azure Portal → Storage Account → Data management → Static website → Enable.
Index document: `index.html`
Error document: `index.html`
